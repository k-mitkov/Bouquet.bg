// Define constant strings for claims
const PERMISSION_BOUQUET_ADD = 'Permission.Bouquet.Add';
const PERMISSION_SHOP_ADD = 'Permission.Shop.Add';
const PERMISSION_WORKER_ADD = 'Permission.Worker.Add';

// Create an object to hold all the claims
const Claims = {
  PERMISSION_BOUQUET_ADD,
  PERMISSION_SHOP_ADD,
  PERMISSION_WORKER_ADD
};

export default Claims;